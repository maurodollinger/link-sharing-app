import Login from "../ui/user-auth/login";
import MainLoginContainer from "../ui/user-auth/main-login-container";

export default function Page() {
    return (
        <MainLoginContainer>
            <Login />
        </MainLoginContainer>
    );
}
